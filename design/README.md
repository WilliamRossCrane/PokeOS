# PokéOS Design

This folder contains UI mockups and visual design assets for PokéOS.

## Structure

- `desktop/` — desktop interface mockups
- `mobile/` — mobile interface mockups

Written design rules are stored in
`docs/06-design-system.md`.
